import React from 'react';
import { Typography } from 'antd';

const { Text } = Typography;

export const ShiftExpensesTab: React.FC = () => {
    return (
        <div className="p-8 text-center">
            <Text className="text-gray-500">مصاريف الوردية</Text>
        </div>
    );
};
